# Google Maps – service area map (Pricing / Testimonials section).
# Enable "Maps JavaScript API" in Google Cloud Console.
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_key_here

# Resend (https://resend.com) – used by the contact/booking form to email you.
# Create an API key in the Resend dashboard. For testing you can use their default "from" address.

# Optional: custom "from" address (e.g. after verifying your domain in Resend).
# RESEND_FROM=Car Detailing <bookings@yourdomain.com>
